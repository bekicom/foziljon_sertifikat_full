# Local Backend Setup

Frontend uses this API URL by default:

```text
http://localhost:5000/
```

For deployment, set:

```env
REACT_APP_API_URL=https://your-backend-domain.com/
```

Local login after backend seed:

```text
admin / 0000
```
